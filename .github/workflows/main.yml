name: CI-DockerBuild-web

on: 
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  build-and-deploy:
    uses: jnsierra/devops-pipelines/.github/workflows/docker-workflow.yml@main
    with:
      docker_image_name: proj-openshift-web
      name_project: proj-openshift-web      
    secrets: inherit
